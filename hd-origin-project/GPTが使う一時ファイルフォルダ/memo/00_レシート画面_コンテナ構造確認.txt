==============================
レシート画面 コンテナ構造確認
==============================
日時: 2026-07-04 17:22:11

対象:
G:\GITHUB\wix-test\hd-origin-project\web_receiver\public\receipts\receipt-list.html

収集先:
G:\GITHUB\wix-test\hd-origin-project\GPTが使う一時ファイルフォルダ\GPTに渡すフォルダ

※今回は確認のみ。receipt-list.html 本体は変更していません。

==============================
CSS/JSブロック数
==============================
支払コンテナCSS RECEIPT_PAYMENT_AMOUNT_CONTAINER_20260704: 0
支払コンテナ高さCSS RECEIPT_PAYMENT_CONTAINER_HEIGHT_ONLY_20260704: 0
支払コンテナ整理CSS RECEIPT_PAYMENT_CONTAINER_CLEAN_OUTER_ONLY_20260704: 1
支払コンテナJS RECEIPT_PAYMENT_AMOUNT_CONTAINER_20260704_SCRIPT: 1
取引日・時刻移動JS RECEIPT_DATE_TIME_IN_VENDOR_CONTAINER_20260704_SCRIPT: 1

==============================
主要コンテナ出現数
==============================
receipt-payment-amount-container: 4
receipt-amount-direct-row: 9
receipt-date-payment-row: 10
receipt-vendor-direct-stack: 20
receipt-master-combo-row: 5
receipt-master-select-row: 22
receipt-target-person-row: 2

==============================
HTML本体の並び確認
==============================

---- line 769: receipt-date-payment-row ----
      box-sizing: border-box !important;
    }

    #formArea .receipt-date-payment-row {
      grid-template-columns: 8em minmax(0, 1fr) !important;
      width: 22em !important;
      max-width: 22em !important;
    }

    #formArea #transactionDate {

---- line 865: receipt-date-payment-row ----
      box-sizing: border-box !important;
    }

    #formArea .receipt-date-payment-row {
      display: grid !important;
      grid-template-columns: 8em minmax(0, 1fr) !important;
      gap: 0.5rem !important;
      width: 100% !important;
      max-width: 100% !important;
      overflow: hidden !important;

---- line 1219: receipt-date-payment-row ----
/* RECEIPT_TIME_AI_MINIMAL_FIX_20260704_END */
/* RECEIPT_TIME_ROW_REGEX_INSERT_20260704_START */

#formArea .receipt-date-payment-row {
  grid-template-columns: 18ch 9ch 26em !important;
}

#formArea #receiptTimeText {
  width: 9ch !important;
  min-width: 9ch !important;

---- line 1370: receipt-date-payment-row ----
      画像表示スペース、画像サイズ、3列レイアウトは触らない。
    */

    #formArea .receipt-date-payment-row {
      display: grid !important;
      grid-template-columns: 18ch !important;
      gap: 0.5rem !important;
      width: max-content !important;
      max-width: 100% !important;
      box-sizing: border-box !important;

---- line 1458: receipt-date-payment-row ----
      - 画像表示スペース、画像サイズ、左中右の3列レイアウトは触らない
    */

    #formArea .receipt-date-payment-row {
      display: grid !important;
      grid-template-columns: 18ch 9ch !important;
      gap: 0.5rem !important;
      align-items: end !important;
      width: max-content !important;
      max-width: 100% !important;

---- line 1483: receipt-date-payment-row ----
      box-sizing: border-box !important;
    }

    #formArea .receipt-date-payment-row > div {
      background: #cfe4ff !important;
      border: 0.12rem solid #6ba7ff !important;
      border-left: 0.4rem solid #1f78d1 !important;
      border-radius: 0.45rem !important;
      padding: 0.35rem !important;
      box-sizing: border-box !important;

---- line 1569: receipt-date-payment-row ----
    }

    @media (max-width: 72rem) {
      #formArea .receipt-date-payment-row {
        grid-template-columns: 1fr 1fr !important;
        width: 100% !important;
      }

      #formArea #transactionDate,
      #formArea #receiptTimeText {

---- line 1624: receipt-date-payment-row ----
      box-sizing: border-box !important;
    }

    #formArea .receipt-date-payment-row:empty {
      display: none !important;
    }

    /* RECEIPT_DATE_TIME_IN_VENDOR_CONTAINER_20260704_END */

    /* RECEIPT_DATE_TIME_FLAT_IN_VENDOR_20260704_START */

---- line 2205: receipt-date-payment-row ----
          <div class="block-body">
            <div class="grid-basic-top-row">
              <div class="receipt-basic-left-stack">
                <div class="receipt-date-payment-row">
                  <div>
                    <label>取引日</label>
                    <input id="transactionDate" type="date" value="${esc(toDateInput(currentDraft.transaction_date, raw))}">
                  </div>
                  
                  <div>

---- line 3973: receipt-date-payment-row ----
          row.appendChild(timeBox);
        }

        var oldDateRow = form.querySelector(".receipt-date-payment-row");
        if (oldDateRow && oldDateRow.children.length === 0) {
          oldDateRow.style.display = "none";
        }
      }

      if (document.readyState === "loading") {

---- line 781: paymentMethodName ----
      max-width: 8em !important;
    }

    #formArea #paymentMethodName {
      width: 13.25em !important;
      min-width: 0 !important;
      max-width: 13.25em !important;
    }

    #formArea .receipt-master-combo-row {

---- line 881: paymentMethodName ----
      max-width: 8em !important;
    }

    #formArea #paymentMethodName {
      width: 100% !important;
      min-width: 0 !important;
      max-width: 100% !important;
    }

    #formArea .receipt-amount-direct-row {

---- line 1413: paymentMethodName ----
      box-sizing: border-box !important;
    }

    #formArea #paymentMethodName,
    #formArea #totalAmount,
    #formArea #taxAmount {
      width: 100% !important;
      min-width: 0 !important;
      max-width: 100% !important;
      box-sizing: border-box !important;

---- line 2216: paymentMethodName ----
                    <input id="receiptTimeText" value="${esc(receiptTimeValueForForm(currentDraft, raw, item))}">
                  </div><div>
                    <label>支払方法</label>
                    <input id="paymentMethodName" value="${esc(currentDraft.payment_method_name)}">
                  </div>
                </div>
                <div class="receipt-amount-direct-row">
                  <div class="receipt-amount-direct-field receipt-amount-total">
                    <label>合計金額</label>
                    <input id="totalAmount" type="number" step="any" value="${esc(numValue(currentDraft.total_amount))}">

---- line 2454: paymentMethodName ----
        taxAmount: cleanNumber(getInput("taxAmount")),
        taxRate: getInput("taxRate"),
        taxTreatmentName: getInput("taxTreatmentName"),
        paymentMethodName: getInput("paymentMethodName"),
        receiptTimeText: getInput("receiptTimeText") || null,
        targetPersonId: getInput("targetPersonId"),
        purposeId: getInput("purposeId"),
        projectId: getInput("projectId"),
        departmentId: getInput("departmentId"),
        invoiceTypeId: getInput("invoiceTypeId"),

---- line 2623: paymentMethodName ----
        taxAmount: cleanNumber(draft.tax_amount),
        taxRate: draft.tax_rate || "",
        taxTreatmentName: draft.tax_treatment_name || raw.taxTreatmentName || raw.tax_treatment_name || "",
        paymentMethodName: draft.payment_method_name || raw.paymentMethodName || raw.payment_method_name || "",
        targetPersonId: draft.target_person_id || raw.targetPersonId || raw.target_person_id || null,
        purposeId: draft.purpose_id || raw.purposeId || raw.purpose_id || null,
        projectId: draft.project_id || raw.projectId || raw.project_id || null,
        departmentId: draft.department_id || raw.departmentId || raw.department_id || null,
        invoiceTypeId: draft.invoice_type_id || raw.invoiceTypeId || raw.invoice_type_id || null,
        evidenceTypeId: draft.evidence_type_id || raw.evidenceTypeId || raw.evidence_type_id || null,

---- line 3703: paymentMethodName ----

        lines.push("【レシート読取結果】");
        lines.push(receiptCopyLine("取引日", receiptCopyGetInput("transactionDate")));
        lines.push(receiptCopyLine("支払方法", receiptCopyGetInput("paymentMethodName")));
        lines.push(receiptCopyLine("店名・支払先", receiptCopyGetInput("vendorName")));
        lines.push(receiptCopyLine("住所", receiptCopyGetInput("vendorAddress")));
        lines.push(receiptCopyLine("電話番号", receiptCopyGetInput("vendorPhone")));
        lines.push(receiptCopyLine("インボイス番号", receiptCopyGetInput("invoiceNumber")));
        lines.push("");


---- line 4018: paymentMethodName ----
        var form = document.getElementById("formArea");
        if (!form) return;

        var payment = form.querySelector("#paymentMethodName");
        var total = form.querySelector("#totalAmount");
        var tax = form.querySelector("#taxAmount");

        if (!payment || !total || !tax) return;

        var paymentBox = closestFieldBox(payment);

---- line 887: receipt-amount-direct-row ----
      max-width: 100% !important;
    }

    #formArea .receipt-amount-direct-row {
      width: 100% !important;
      max-width: 100% !important;
      margin-top: 0.45rem !important;
      overflow: hidden !important;
      box-sizing: border-box !important;
    }

---- line 1379: receipt-amount-direct-row ----
      box-sizing: border-box !important;
    }

    #formArea .receipt-amount-direct-row {
      display: grid !important;
      grid-template-columns: 14rem 10rem 9rem max-content !important;
      gap: 0.5rem !important;
      align-items: end !important;
      justify-content: start !important;
      width: max-content !important;

---- line 1434: receipt-amount-direct-row ----
    }

    @media (max-width: 86rem) {
      #formArea .receipt-amount-direct-row {
        grid-template-columns: 1fr 1fr !important;
        width: 100% !important;
      }

      #formArea .receipt-payment-field,
      #formArea .receipt-amount-total,

---- line 1501: receipt-amount-direct-row ----
      box-sizing: border-box !important;
    }

    #formArea .receipt-amount-direct-row > div,
    #formArea .receipt-amount-direct-field {
      background: #cfeeda !important;
      border: 0.12rem solid #61b878 !important;
      border-left: 0.4rem solid #248a43 !important;
      border-radius: 0.45rem !important;
      padding: 0.35rem !important;

---- line 1732: receipt-amount-direct-row ----
      overflow: visible !important;
    }

    #formArea .receipt-amount-direct-row:empty {
      display: none !important;
    }

    @media (max-width: 86rem) {
      #formArea .receipt-payment-amount-container {
        width: 100% !important;

---- line 2219: receipt-amount-direct-row ----
                    <input id="paymentMethodName" value="${esc(currentDraft.payment_method_name)}">
                  </div>
                </div>
                <div class="receipt-amount-direct-row">
                  <div class="receipt-amount-direct-field receipt-amount-total">
                    <label>合計金額</label>
                    <input id="totalAmount" type="number" step="any" value="${esc(numValue(currentDraft.total_amount))}">
                  </div>
                  <div class="receipt-amount-direct-field receipt-amount-tax">
                    <label>消費税合計</label>

---- line 4056: receipt-amount-direct-row ----
          container.className = "receipt-payment-amount-container";

          var amountRow =
            totalBox.closest(".receipt-amount-direct-row") ||
            paymentBox.closest(".receipt-amount-direct-row") ||
            taxBox.closest(".receipt-amount-direct-row");

          if (amountRow && amountRow.parentNode) {
            amountRow.parentNode.insertBefore(container, amountRow);
          } else {

---- line 4057: receipt-amount-direct-row ----

          var amountRow =
            totalBox.closest(".receipt-amount-direct-row") ||
            paymentBox.closest(".receipt-amount-direct-row") ||
            taxBox.closest(".receipt-amount-direct-row");

          if (amountRow && amountRow.parentNode) {
            amountRow.parentNode.insertBefore(container, amountRow);
          } else {
            var vendorStack = form.querySelector(".receipt-vendor-direct-stack");

---- line 4058: receipt-amount-direct-row ----
          var amountRow =
            totalBox.closest(".receipt-amount-direct-row") ||
            paymentBox.closest(".receipt-amount-direct-row") ||
            taxBox.closest(".receipt-amount-direct-row");

          if (amountRow && amountRow.parentNode) {
            amountRow.parentNode.insertBefore(container, amountRow);
          } else {
            var vendorStack = form.querySelector(".receipt-vendor-direct-stack");
            if (vendorStack && vendorStack.parentNode) {

---- line 1414: totalAmount ----
    }

    #formArea #paymentMethodName,
    #formArea #totalAmount,
    #formArea #taxAmount {
      width: 100% !important;
      min-width: 0 !important;
      max-width: 100% !important;
      box-sizing: border-box !important;
    }

---- line 1422: totalAmount ----
      box-sizing: border-box !important;
    }

    #formArea #totalAmount,
    #formArea #taxAmount {
      text-align: right !important;
      font-variant-numeric: tabular-nums !important;
    }

    #formArea #taxBreakdownButton {

---- line 2222: totalAmount ----
                <div class="receipt-amount-direct-row">
                  <div class="receipt-amount-direct-field receipt-amount-total">
                    <label>合計金額</label>
                    <input id="totalAmount" type="number" step="any" value="${esc(numValue(currentDraft.total_amount))}">
                  </div>
                  <div class="receipt-amount-direct-field receipt-amount-tax">
                    <label>消費税合計</label>
                    <input id="taxAmount" type="number" step="any" value="${esc(numValue(currentDraft.tax_amount))}">
                  </div>
                  <button type="button" id="taxBreakdownButton" onclick="openTaxBreakdown()">消費税内訳</button>

---- line 2450: totalAmount ----
        vendorName: getInput("vendorName"),
                vendorAddress: getInput("vendorAddress"),
        vendorPhone: getInput("vendorPhone"),
totalAmount: cleanNumber(getInput("totalAmount")),
        taxAmount: cleanNumber(getInput("taxAmount")),
        taxRate: getInput("taxRate"),
        taxTreatmentName: getInput("taxTreatmentName"),
        paymentMethodName: getInput("paymentMethodName"),
        receiptTimeText: getInput("receiptTimeText") || null,
        targetPersonId: getInput("targetPersonId"),

---- line 2619: totalAmount ----
        vendorName: draft.vendor_name || raw.vendorName || raw.vendor_name || "",
        vendorAddress: draft.vendor_address || raw.vendorAddress || raw.vendor_address || "",
        vendorPhone: draft.vendor_phone || raw.vendorPhone || raw.vendor_phone || "",
        totalAmount: cleanNumber(draft.total_amount),
        taxAmount: cleanNumber(draft.tax_amount),
        taxRate: draft.tax_rate || "",
        taxTreatmentName: draft.tax_treatment_name || raw.taxTreatmentName || raw.tax_treatment_name || "",
        paymentMethodName: draft.payment_method_name || raw.paymentMethodName || raw.payment_method_name || "",
        targetPersonId: draft.target_person_id || raw.targetPersonId || raw.target_person_id || null,
        purposeId: draft.purpose_id || raw.purposeId || raw.purpose_id || null,

---- line 3162: totalAmount ----
            "レシート";

          const amount =
            findValueDeep(source, ["totalAmount", "total_amount", "amount", "税込金額", "支払総額"]) ||
            "";

          const amountText = formatYenForList(amount);

          const label =
            "ID" + id + "　" +

---- line 3711: totalAmount ----
        lines.push("");

        lines.push("【金額】");
        lines.push(receiptCopyLine("合計金額", receiptCopyGetInput("totalAmount")));
        lines.push(receiptCopyLine("消費税合計", receiptCopyGetInput("taxAmount")));
        lines.push(receiptCopyLine("税率", receiptCopyGetInput("taxRate")));
        lines.push(receiptCopyLine("税処理", receiptCopyGetInput("taxTreatmentName")));
        lines.push("");

        lines.push("【分類】");

---- line 4019: totalAmount ----
        if (!form) return;

        var payment = form.querySelector("#paymentMethodName");
        var total = form.querySelector("#totalAmount");
        var tax = form.querySelector("#taxAmount");

        if (!payment || !total || !tax) return;

        var paymentBox = closestFieldBox(payment);
        var totalBox = closestFieldBox(total);

---- line 490: taxAmount ----
      width: 100% !important;
    }

    #taxAmount {
      width: 100% !important;
      min-width: 8em !important;
    }

    #taxBreakdownButton {
      white-space: nowrap !important;

---- line 1415: taxAmount ----

    #formArea #paymentMethodName,
    #formArea #totalAmount,
    #formArea #taxAmount {
      width: 100% !important;
      min-width: 0 !important;
      max-width: 100% !important;
      box-sizing: border-box !important;
    }


---- line 1423: taxAmount ----
    }

    #formArea #totalAmount,
    #formArea #taxAmount {
      text-align: right !important;
      font-variant-numeric: tabular-nums !important;
    }

    #formArea #taxBreakdownButton {
      white-space: nowrap !important;

---- line 2226: taxAmount ----
                  </div>
                  <div class="receipt-amount-direct-field receipt-amount-tax">
                    <label>消費税合計</label>
                    <input id="taxAmount" type="number" step="any" value="${esc(numValue(currentDraft.tax_amount))}">
                  </div>
                  <button type="button" id="taxBreakdownButton" onclick="openTaxBreakdown()">消費税内訳</button>
                </div>
                  <div class="receipt-target-person-row">
                    <label>対象者</label>
                    <select id="targetPersonId">

---- line 2451: taxAmount ----
                vendorAddress: getInput("vendorAddress"),
        vendorPhone: getInput("vendorPhone"),
totalAmount: cleanNumber(getInput("totalAmount")),
        taxAmount: cleanNumber(getInput("taxAmount")),
        taxRate: getInput("taxRate"),
        taxTreatmentName: getInput("taxTreatmentName"),
        paymentMethodName: getInput("paymentMethodName"),
        receiptTimeText: getInput("receiptTimeText") || null,
        targetPersonId: getInput("targetPersonId"),
        purposeId: getInput("purposeId"),

---- line 2620: taxAmount ----
        vendorAddress: draft.vendor_address || raw.vendorAddress || raw.vendor_address || "",
        vendorPhone: draft.vendor_phone || raw.vendorPhone || raw.vendor_phone || "",
        totalAmount: cleanNumber(draft.total_amount),
        taxAmount: cleanNumber(draft.tax_amount),
        taxRate: draft.tax_rate || "",
        taxTreatmentName: draft.tax_treatment_name || raw.taxTreatmentName || raw.tax_treatment_name || "",
        paymentMethodName: draft.payment_method_name || raw.paymentMethodName || raw.payment_method_name || "",
        targetPersonId: draft.target_person_id || raw.targetPersonId || raw.target_person_id || null,
        purposeId: draft.purpose_id || raw.purposeId || raw.purpose_id || null,
        projectId: draft.project_id || raw.projectId || raw.project_id || null,

---- line 3323: taxAmount ----
        taxTreatmentId: "",
        taxTreatmentName: "",
        targetAmount: "",
        taxAmount: "",
        isConfirmed: true,
        sortOrder: (receiptTaxBreakdowns.length + 1) * 10
      };
    }

    function mapTaxBreakdownFromApi(row) {

---- line 3338: taxAmount ----
        taxTreatmentId: String(row.tax_treatment_id || row.taxTreatmentId || ""),
        taxTreatmentName: String(row.tax_treatment_name || row.taxTreatmentName || ""),
        targetAmount: row.target_amount !== null && row.target_amount !== undefined ? String(row.target_amount) : String(row.targetAmount || ""),
        taxAmount: row.tax_amount !== null && row.tax_amount !== undefined ? String(row.tax_amount) : String(row.taxAmount || ""),
        aiConfidence: row.ai_confidence ?? row.aiConfidence ?? null,
        isConfirmed: row.is_confirmed === false || row.isConfirmed === false ? false : true,
        sortOrder: Number(row.sort_order || row.sortOrder || 0)
      };
    }


---- line 3349: taxAmount ----
      return String(row.taxCategoryId || "").trim() ||
        String(row.taxTreatmentId || "").trim() ||
        String(row.targetAmount || "").trim() ||
        String(row.taxAmount || "").trim();
    }

    function getTaxBreakdownsForSave() {
      return receiptTaxBreakdowns
        .filter(hasMeaningTaxBreakdownRow)
        .map((row, index) => {

---- line 3363: taxAmount ----
            taxTreatmentId: row.taxTreatmentId || null,
            taxTreatmentName: row.taxTreatmentName || "",
            targetAmount: row.targetAmount || "",
            taxAmount: row.taxAmount || "",
            isConfirmed: true,
            sortOrder: row.sortOrder || (index + 1) * 10
          };
        });
    }


---- line 3492: taxAmount ----
              <input data-field="targetAmount" type="number" value="${esc(numValue(row.targetAmount))}" oninput="onTaxBreakdownInput(${index}, 'targetAmount', this.value)">
            </td>
            <td>
              <input data-field="taxAmount" type="number" value="${esc(numValue(row.taxAmount))}" oninput="onTaxBreakdownInput(${index}, 'taxAmount', this.value)">
            </td>
            <td>
              <select data-field="taxTreatmentId" onchange="onTaxBreakdownTreatmentChange(${index}, this.value)">
                ${optionHtml(taxTreatments, "tax_treatment_id", "treatment_name", row.taxTreatmentId)}
              </select>
            </td>

---- line 3539: taxAmount ----

    function updateTaxBreakdownTotalText() {
      const total = receiptTaxBreakdowns.reduce((sum, row) => {
        return sum + toNumberValue(row.taxAmount);
      }, 0);

      const el = document.getElementById("taxBreakdownTotalText");
      if (el) {
        el.textContent = "消費税合計：" + formatYen(total);
      }

---- line 3552: taxAmount ----
      receiptTaxBreakdowns.forEach((row) => { row.isConfirmed = true; });

      const total = receiptTaxBreakdowns.reduce((sum, row) => {
        return sum + toNumberValue(row.taxAmount);
      }, 0);

      const taxAmount = document.getElementById("taxAmount");
      if (taxAmount) {
        taxAmount.value = total ? String(Math.round(total)) : "";
      }

---- line 3555: taxAmount ----
        return sum + toNumberValue(row.taxAmount);
      }, 0);

      const taxAmount = document.getElementById("taxAmount");
      if (taxAmount) {
        taxAmount.value = total ? String(Math.round(total)) : "";
      }

      const activeRows = receiptTaxBreakdowns.filter(hasMeaningTaxBreakdownRow);


---- line 3556: taxAmount ----
      }, 0);

      const taxAmount = document.getElementById("taxAmount");
      if (taxAmount) {
        taxAmount.value = total ? String(Math.round(total)) : "";
      }

      const activeRows = receiptTaxBreakdowns.filter(hasMeaningTaxBreakdownRow);

      const taxRateInput = document.getElementById("taxRate");

---- line 3557: taxAmount ----

      const taxAmount = document.getElementById("taxAmount");
      if (taxAmount) {
        taxAmount.value = total ? String(Math.round(total)) : "";
      }

      const activeRows = receiptTaxBreakdowns.filter(hasMeaningTaxBreakdownRow);

      const taxRateInput = document.getElementById("taxRate");
      if (taxRateInput) {

---- line 3712: taxAmount ----

        lines.push("【金額】");
        lines.push(receiptCopyLine("合計金額", receiptCopyGetInput("totalAmount")));
        lines.push(receiptCopyLine("消費税合計", receiptCopyGetInput("taxAmount")));
        lines.push(receiptCopyLine("税率", receiptCopyGetInput("taxRate")));
        lines.push(receiptCopyLine("税処理", receiptCopyGetInput("taxTreatmentName")));
        lines.push("");

        lines.push("【分類】");
        lines.push(receiptCopyLine("対象者", receiptCopyGetInput("targetPersonId")));

---- line 4020: taxAmount ----

        var payment = form.querySelector("#paymentMethodName");
        var total = form.querySelector("#totalAmount");
        var tax = form.querySelector("#taxAmount");

        if (!payment || !total || !tax) return;

        var paymentBox = closestFieldBox(payment);
        var totalBox = closestFieldBox(total);
        var taxBox = closestFieldBox(tax);

---- line 495: taxBreakdownButton ----
      min-width: 8em !important;
    }

    #taxBreakdownButton {
      white-space: nowrap !important;
      height: 2.4rem !important;
      padding: 0 0.8rem !important;
    }

    #formArea .grid-tax-row > div:has(#taxRate),

---- line 1428: taxBreakdownButton ----
      font-variant-numeric: tabular-nums !important;
    }

    #formArea #taxBreakdownButton {
      white-space: nowrap !important;
      align-self: end !important;
    }

    @media (max-width: 86rem) {
      #formArea .receipt-amount-direct-row {

---- line 2228: taxBreakdownButton ----
                    <label>消費税合計</label>
                    <input id="taxAmount" type="number" step="any" value="${esc(numValue(currentDraft.tax_amount))}">
                  </div>
                  <button type="button" id="taxBreakdownButton" onclick="openTaxBreakdown()">消費税内訳</button>
                </div>
                  <div class="receipt-target-person-row">
                    <label>対象者</label>
                    <select id="targetPersonId">
                      ${optionHtml((receiptMasterOptions || {}).targetPeople || [], "target_person_id", "target_person_name", currentDraft.target_person_id)}
                    </select>

---- line 4031: taxBreakdownButton ----
        if (!paymentBox || !totalBox || !taxBox) return;

        var taxButton =
          form.querySelector("#taxBreakdownButton") ||
          form.querySelector("[data-tax-breakdown-button]") ||
          Array.prototype.slice.call(form.querySelectorAll("button")).find(function (button) {
            return String(button.textContent || "").indexOf("内訳") >= 0;
          });

        var taxButtonBox = null;

---- line 903: targetPersonId ----
      box-sizing: border-box !important;
    }

    #formArea #targetPersonId {
      width: 100% !important;
      min-width: 0 !important;
      max-width: 100% !important;
      box-sizing: border-box !important;
    }


---- line 2232: targetPersonId ----
                </div>
                  <div class="receipt-target-person-row">
                    <label>対象者</label>
                    <select id="targetPersonId">
                      ${optionHtml((receiptMasterOptions || {}).targetPeople || [], "target_person_id", "target_person_name", currentDraft.target_person_id)}
                    </select>
                  </div>




---- line 2456: targetPersonId ----
        taxTreatmentName: getInput("taxTreatmentName"),
        paymentMethodName: getInput("paymentMethodName"),
        receiptTimeText: getInput("receiptTimeText") || null,
        targetPersonId: getInput("targetPersonId"),
        purposeId: getInput("purposeId"),
        projectId: getInput("projectId"),
        departmentId: getInput("departmentId"),
        invoiceTypeId: getInput("invoiceTypeId"),
        evidenceTypeId: getInput("evidenceTypeId"),
        evidenceMemo: getInput("evidenceMemo"),

---- line 2624: targetPersonId ----
        taxRate: draft.tax_rate || "",
        taxTreatmentName: draft.tax_treatment_name || raw.taxTreatmentName || raw.tax_treatment_name || "",
        paymentMethodName: draft.payment_method_name || raw.paymentMethodName || raw.payment_method_name || "",
        targetPersonId: draft.target_person_id || raw.targetPersonId || raw.target_person_id || null,
        purposeId: draft.purpose_id || raw.purposeId || raw.purpose_id || null,
        projectId: draft.project_id || raw.projectId || raw.project_id || null,
        departmentId: draft.department_id || raw.departmentId || raw.department_id || null,
        invoiceTypeId: draft.invoice_type_id || raw.invoiceTypeId || raw.invoice_type_id || null,
        evidenceTypeId: draft.evidence_type_id || raw.evidenceTypeId || raw.evidence_type_id || null,
        evidenceMemo: draft.evidence_memo || raw.evidenceMemo || raw.evidence_memo || "",

---- line 3718: targetPersonId ----
        lines.push("");

        lines.push("【分類】");
        lines.push(receiptCopyLine("対象者", receiptCopyGetInput("targetPersonId")));
        lines.push(receiptCopyLine("目的", receiptCopyMasterValue("purposeId", "purposeTempName")));
        lines.push(receiptCopyLine("案件", receiptCopyMasterValue("projectId", "projectTempName")));
        lines.push(receiptCopyLine("部門", receiptCopyMasterValue("departmentId", "departmentTempName")));
        lines.push(receiptCopyLine("インボイス区分", receiptCopyGetInput("invoiceTypeId")));
        lines.push(receiptCopyLine("証憑区分", receiptCopyGetInput("evidenceTypeId")));
        lines.push(receiptCopyLine("証憑メモ", receiptCopyGetInput("evidenceMemo")));

---- line 699: receipt-master-combo-row ----

    /* RECEIPT_TEMP_MASTER_INPUT_LAYOUT_20260703_START */

    #formArea .receipt-master-combo-row {
      display: grid !important;
      grid-template-columns: repeat(3, minmax(13em, 1fr)) !important;
      gap: 0.5rem !important;
      width: 100% !important;
      margin-top: 0.45rem !important;
    }

---- line 787: receipt-master-combo-row ----
      max-width: 13.25em !important;
    }

    #formArea .receipt-master-combo-row {
      display: grid !important;
      grid-template-columns: minmax(0, 1fr) !important;
      width: 22em !important;
      min-width: 0 !important;
      max-width: 22em !important;
      gap: 0.45rem !important;

---- line 910: receipt-master-combo-row ----
      box-sizing: border-box !important;
    }

    #formArea .receipt-basic-left-stack .receipt-master-combo-row {
      display: grid !important;
      grid-template-columns: minmax(0, 1fr) !important;
      width: 100% !important;
      max-width: 100% !important;
      margin-top: 0.45rem !important;
      overflow: hidden !important;

---- line 1511: receipt-master-combo-row ----
      box-sizing: border-box !important;
    }

    #formArea .receipt-master-combo-row > div,
    #formArea .receipt-master-select-row > div {
      background: #e3d4ff !important;
      border: 0.12rem solid #9c7ade !important;
      border-left: 0.4rem solid #6742b8 !important;
      border-radius: 0.45rem !important;
      padding: 0.35rem !important;

---- line 2239: receipt-master-combo-row ----



                <div class="receipt-master-combo-row">
                  <div class="receipt-master-combo-field">
                    <label>目的</label>
                    <div class="receipt-master-action-line">
                      <select id="purposeId">
                        ${optionHtml((receiptMasterOptions || {}).purposes || [], "purpose_id", "purpose_name", currentDraft.purpose_id)}
                      </select>

---- line 2243: purposeId ----
                  <div class="receipt-master-combo-field">
                    <label>目的</label>
                    <div class="receipt-master-action-line">
                      <select id="purposeId">
                        ${optionHtml((receiptMasterOptions || {}).purposes || [], "purpose_id", "purpose_name", currentDraft.purpose_id)}
                      </select>
                      <button type="button" onclick="registerReceiptMasterFromTemp('purpose')">新規登録</button>
                      <button type="button" onclick="focusReceiptTempInput('purpose')">一時入力</button>
                    </div>
                    <input id="purposeTempName" class="receipt-temp-master-input" placeholder="目的の一時入力" value="${esc(currentDraft.purpose_temp_name || raw.purposeTempName || raw.purpose_temp_name || "")}">

---- line 2457: purposeId ----
        paymentMethodName: getInput("paymentMethodName"),
        receiptTimeText: getInput("receiptTimeText") || null,
        targetPersonId: getInput("targetPersonId"),
        purposeId: getInput("purposeId"),
        projectId: getInput("projectId"),
        departmentId: getInput("departmentId"),
        invoiceTypeId: getInput("invoiceTypeId"),
        evidenceTypeId: getInput("evidenceTypeId"),
        evidenceMemo: getInput("evidenceMemo"),
        accountTitleName: getInput("accountTitleName"),

---- line 2625: purposeId ----
        taxTreatmentName: draft.tax_treatment_name || raw.taxTreatmentName || raw.tax_treatment_name || "",
        paymentMethodName: draft.payment_method_name || raw.paymentMethodName || raw.payment_method_name || "",
        targetPersonId: draft.target_person_id || raw.targetPersonId || raw.target_person_id || null,
        purposeId: draft.purpose_id || raw.purposeId || raw.purpose_id || null,
        projectId: draft.project_id || raw.projectId || raw.project_id || null,
        departmentId: draft.department_id || raw.departmentId || raw.department_id || null,
        invoiceTypeId: draft.invoice_type_id || raw.invoiceTypeId || raw.invoice_type_id || null,
        evidenceTypeId: draft.evidence_type_id || raw.evidenceTypeId || raw.evidence_type_id || null,
        evidenceMemo: draft.evidence_memo || raw.evidenceMemo || raw.evidence_memo || "",
        accountTitleName: draft.account_title_name || "",

---- line 3719: purposeId ----

        lines.push("【分類】");
        lines.push(receiptCopyLine("対象者", receiptCopyGetInput("targetPersonId")));
        lines.push(receiptCopyLine("目的", receiptCopyMasterValue("purposeId", "purposeTempName")));
        lines.push(receiptCopyLine("案件", receiptCopyMasterValue("projectId", "projectTempName")));
        lines.push(receiptCopyLine("部門", receiptCopyMasterValue("departmentId", "departmentTempName")));
        lines.push(receiptCopyLine("インボイス区分", receiptCopyGetInput("invoiceTypeId")));
        lines.push(receiptCopyLine("証憑区分", receiptCopyGetInput("evidenceTypeId")));
        lines.push(receiptCopyLine("証憑メモ", receiptCopyGetInput("evidenceMemo")));
        lines.push("");

---- line 2254: projectId ----
                  <div class="receipt-master-combo-field">
                    <label>案件</label>
                    <div class="receipt-master-action-line">
                      <select id="projectId">
                        ${optionHtml((receiptMasterOptions || {}).projects || [], "project_id", "project_name", currentDraft.project_id)}
                      </select>
                      <button type="button" onclick="registerReceiptMasterFromTemp('project')">新規登録</button>
                      <button type="button" onclick="focusReceiptTempInput('project')">一時入力</button>
                    </div>
                    <input id="projectTempName" class="receipt-temp-master-input" placeholder="案件の一時入力" value="${esc(currentDraft.project_temp_name || raw.projectTempName || raw.project_temp_name || "")}">

---- line 2458: projectId ----
        receiptTimeText: getInput("receiptTimeText") || null,
        targetPersonId: getInput("targetPersonId"),
        purposeId: getInput("purposeId"),
        projectId: getInput("projectId"),
        departmentId: getInput("departmentId"),
        invoiceTypeId: getInput("invoiceTypeId"),
        evidenceTypeId: getInput("evidenceTypeId"),
        evidenceMemo: getInput("evidenceMemo"),
        accountTitleName: getInput("accountTitleName"),
        invoiceNumber: getInput("invoiceNumber"),

---- line 2626: projectId ----
        paymentMethodName: draft.payment_method_name || raw.paymentMethodName || raw.payment_method_name || "",
        targetPersonId: draft.target_person_id || raw.targetPersonId || raw.target_person_id || null,
        purposeId: draft.purpose_id || raw.purposeId || raw.purpose_id || null,
        projectId: draft.project_id || raw.projectId || raw.project_id || null,
        departmentId: draft.department_id || raw.departmentId || raw.department_id || null,
        invoiceTypeId: draft.invoice_type_id || raw.invoiceTypeId || raw.invoice_type_id || null,
        evidenceTypeId: draft.evidence_type_id || raw.evidenceTypeId || raw.evidence_type_id || null,
        evidenceMemo: draft.evidence_memo || raw.evidenceMemo || raw.evidence_memo || "",
        accountTitleName: draft.account_title_name || "",
        invoiceNumber: draft.invoice_number || raw.invoiceNumber || raw.invoice_number || "",

---- line 3720: projectId ----
        lines.push("【分類】");
        lines.push(receiptCopyLine("対象者", receiptCopyGetInput("targetPersonId")));
        lines.push(receiptCopyLine("目的", receiptCopyMasterValue("purposeId", "purposeTempName")));
        lines.push(receiptCopyLine("案件", receiptCopyMasterValue("projectId", "projectTempName")));
        lines.push(receiptCopyLine("部門", receiptCopyMasterValue("departmentId", "departmentTempName")));
        lines.push(receiptCopyLine("インボイス区分", receiptCopyGetInput("invoiceTypeId")));
        lines.push(receiptCopyLine("証憑区分", receiptCopyGetInput("evidenceTypeId")));
        lines.push(receiptCopyLine("証憑メモ", receiptCopyGetInput("evidenceMemo")));
        lines.push("");


---- line 2265: departmentId ----
                  <div class="receipt-master-combo-field">
                    <label>部門</label>
                    <div class="receipt-master-action-line">
                      <select id="departmentId">
                        ${optionHtml((receiptMasterOptions || {}).departments || [], "department_id", "department_name", currentDraft.department_id)}
                      </select>
                      <button type="button" onclick="registerReceiptMasterFromTemp('department')">新規登録</button>
                      <button type="button" onclick="focusReceiptTempInput('department')">一時入力</button>
                    </div>
                    <input id="departmentTempName" class="receipt-temp-master-input" placeholder="部門の一時入力" value="${esc(currentDraft.department_temp_name || raw.departmentTempName || raw.department_temp_name || "")}">

---- line 2459: departmentId ----
        targetPersonId: getInput("targetPersonId"),
        purposeId: getInput("purposeId"),
        projectId: getInput("projectId"),
        departmentId: getInput("departmentId"),
        invoiceTypeId: getInput("invoiceTypeId"),
        evidenceTypeId: getInput("evidenceTypeId"),
        evidenceMemo: getInput("evidenceMemo"),
        accountTitleName: getInput("accountTitleName"),
        invoiceNumber: getInput("invoiceNumber"),
        summary: getInput("summary"),

---- line 2627: departmentId ----
        targetPersonId: draft.target_person_id || raw.targetPersonId || raw.target_person_id || null,
        purposeId: draft.purpose_id || raw.purposeId || raw.purpose_id || null,
        projectId: draft.project_id || raw.projectId || raw.project_id || null,
        departmentId: draft.department_id || raw.departmentId || raw.department_id || null,
        invoiceTypeId: draft.invoice_type_id || raw.invoiceTypeId || raw.invoice_type_id || null,
        evidenceTypeId: draft.evidence_type_id || raw.evidenceTypeId || raw.evidence_type_id || null,
        evidenceMemo: draft.evidence_memo || raw.evidenceMemo || raw.evidence_memo || "",
        accountTitleName: draft.account_title_name || "",
        invoiceNumber: draft.invoice_number || raw.invoiceNumber || raw.invoice_number || "",
        summary: draft.summary || "",

---- line 3721: departmentId ----
        lines.push(receiptCopyLine("対象者", receiptCopyGetInput("targetPersonId")));
        lines.push(receiptCopyLine("目的", receiptCopyMasterValue("purposeId", "purposeTempName")));
        lines.push(receiptCopyLine("案件", receiptCopyMasterValue("projectId", "projectTempName")));
        lines.push(receiptCopyLine("部門", receiptCopyMasterValue("departmentId", "departmentTempName")));
        lines.push(receiptCopyLine("インボイス区分", receiptCopyGetInput("invoiceTypeId")));
        lines.push(receiptCopyLine("証憑区分", receiptCopyGetInput("evidenceTypeId")));
        lines.push(receiptCopyLine("証憑メモ", receiptCopyGetInput("evidenceMemo")));
        lines.push("");

        lines.push("【摘要・メモ】");

---- line 761: receipt-vendor-direct-stack ----
      box-sizing: border-box !important;
    }

    #formArea .receipt-vendor-direct-stack {
      width: 24em !important;
      min-width: 0 !important;
      max-width: 24em !important;
      overflow: hidden !important;
      box-sizing: border-box !important;
    }

---- line 858: receipt-vendor-direct-stack ----
    }

    #formArea .receipt-basic-left-stack,
    #formArea .receipt-vendor-direct-stack {
      min-width: 0 !important;
      max-width: 100% !important;
      overflow: hidden !important;
      box-sizing: border-box !important;
    }


---- line 920: receipt-vendor-direct-stack ----
      box-sizing: border-box !important;
    }

    #formArea .receipt-vendor-direct-stack .receipt-master-select-row {
      display: grid !important;
      grid-template-columns: minmax(0, 1fr) !important;
      width: 100% !important;
      max-width: 100% !important;
      margin-top: 0.45rem !important;
      overflow: hidden !important;

---- line 961: receipt-vendor-direct-stack ----
      右側の店名・支払先ブロック内、インボイス番号の下に置く。
    */

    #formArea .receipt-vendor-direct-stack .receipt-master-select-row {
      display: grid !important;
      grid-template-columns: minmax(0, 1fr) !important;
      width: 100% !important;
      min-width: 0 !important;
      max-width: 100% !important;
      gap: 0.45rem !important;

---- line 973: receipt-vendor-direct-stack ----
      box-sizing: border-box !important;
    }

    #formArea .receipt-vendor-direct-stack .receipt-master-select-row > div {
      width: 100% !important;
      min-width: 0 !important;
      max-width: 100% !important;
      box-sizing: border-box !important;
    }


---- line 980: receipt-vendor-direct-stack ----
      box-sizing: border-box !important;
    }

    #formArea .receipt-vendor-direct-stack .receipt-master-select-row select,
    #formArea .receipt-vendor-direct-stack .receipt-master-select-row input {
      width: 100% !important;
      min-width: 0 !important;
      max-width: 100% !important;
      box-sizing: border-box !important;
    }

---- line 981: receipt-vendor-direct-stack ----
    }

    #formArea .receipt-vendor-direct-stack .receipt-master-select-row select,
    #formArea .receipt-vendor-direct-stack .receipt-master-select-row input {
      width: 100% !important;
      min-width: 0 !important;
      max-width: 100% !important;
      box-sizing: border-box !important;
    }


---- line 996: receipt-vendor-direct-stack ----

    /* RECEIPT_INVOICE_EVIDENCE_RIGHT_FORCE_FIX_2_20260703_START */

    #formArea .receipt-vendor-direct-stack .receipt-master-select-row {
      display: grid !important;
      grid-template-columns: minmax(0, 1fr) !important;
      width: 100% !important;
      min-width: 0 !important;
      max-width: 100% !important;
      gap: 0.45rem !important;

---- line 1008: receipt-vendor-direct-stack ----
      box-sizing: border-box !important;
    }

    #formArea .receipt-vendor-direct-stack .receipt-master-select-row > div,
    #formArea .receipt-vendor-direct-stack .receipt-master-select-row select,
    #formArea .receipt-vendor-direct-stack .receipt-master-select-row input {
      width: 100% !important;
      min-width: 0 !important;
      max-width: 100% !important;
      box-sizing: border-box !important;

---- line 1009: receipt-vendor-direct-stack ----
    }

    #formArea .receipt-vendor-direct-stack .receipt-master-select-row > div,
    #formArea .receipt-vendor-direct-stack .receipt-master-select-row select,
    #formArea .receipt-vendor-direct-stack .receipt-master-select-row input {
      width: 100% !important;
      min-width: 0 !important;
      max-width: 100% !important;
      box-sizing: border-box !important;
    }

---- line 1010: receipt-vendor-direct-stack ----

    #formArea .receipt-vendor-direct-stack .receipt-master-select-row > div,
    #formArea .receipt-vendor-direct-stack .receipt-master-select-row select,
    #formArea .receipt-vendor-direct-stack .receipt-master-select-row input {
      width: 100% !important;
      min-width: 0 !important;
      max-width: 100% !important;
      box-sizing: border-box !important;
    }


---- line 1331: receipt-vendor-direct-stack ----
    }

    #formArea .receipt-basic-left-stack,
    #formArea .receipt-vendor-direct-stack {
      min-width: 0 !important;
      max-width: 100% !important;
      box-sizing: border-box !important;
      overflow: hidden !important;
    }


---- line 1342: receipt-vendor-direct-stack ----
      justify-self: start !important;
    }

    #formArea .receipt-vendor-direct-stack {
      justify-self: end !important;
      margin-left: auto !important;
      margin-right: 0 !important;
      position: static !important;
      left: auto !important;
      right: auto !important;

---- line 1352: receipt-vendor-direct-stack ----
      transform: none !important;
    }

    #formArea .receipt-vendor-direct-stack input,
    #formArea .receipt-vendor-direct-stack select,
    #formArea .receipt-vendor-direct-stack textarea {
      max-width: 100% !important;
      min-width: 0 !important;
      box-sizing: border-box !important;
    }

---- line 1353: receipt-vendor-direct-stack ----
    }

    #formArea .receipt-vendor-direct-stack input,
    #formArea .receipt-vendor-direct-stack select,
    #formArea .receipt-vendor-direct-stack textarea {
      max-width: 100% !important;
      min-width: 0 !important;
      box-sizing: border-box !important;
    }


---- line 1354: receipt-vendor-direct-stack ----

    #formArea .receipt-vendor-direct-stack input,
    #formArea .receipt-vendor-direct-stack select,
    #formArea .receipt-vendor-direct-stack textarea {
      max-width: 100% !important;
      min-width: 0 !important;
      box-sizing: border-box !important;
    }

    /* RECEIPT_VENDOR_OVERFLOW_STOP_20260704_END */

---- line 1492: receipt-vendor-direct-stack ----
      box-sizing: border-box !important;
    }

    #formArea .receipt-vendor-direct-stack {
      background: #ffe2b8 !important;
      border: 0.12rem solid #e79b31 !important;
      border-left: 0.4rem solid #c96d00 !important;
      border-radius: 0.45rem !important;
      padding: 0.35rem !important;
      box-sizing: border-box !important;

---- line 2279: receipt-vendor-direct-stack ----

              </div>

              <div class="receipt-vendor-direct-stack">
                <label>店名・支払先</label>
                <input id="vendorName" value="${esc(currentDraft.vendor_name)}">

                <label>住所</label>
                <input id="vendorAddress" value="${esc(currentDraft.vendor_address || raw.vendorAddress || raw.vendor_address || "")}">


---- line 3944: receipt-vendor-direct-stack ----
        var form = document.getElementById("formArea");
        if (!form) return;

        var vendorStack = form.querySelector(".receipt-vendor-direct-stack");
        if (!vendorStack) return;

        var transactionDate = form.querySelector("#transactionDate");
        var receiptTimeText = form.querySelector("#receiptTimeText");

        if (!transactionDate || !receiptTimeText) return;

---- line 4063: receipt-vendor-direct-stack ----
          if (amountRow && amountRow.parentNode) {
            amountRow.parentNode.insertBefore(container, amountRow);
          } else {
            var vendorStack = form.querySelector(".receipt-vendor-direct-stack");
            if (vendorStack && vendorStack.parentNode) {
              vendorStack.parentNode.insertBefore(container, vendorStack.nextSibling);
            } else {
              form.insertBefore(container, form.firstChild);
            }
          }

==============================
支払コンテナJS確認
==============================

---- line 4014: function movePaymentAmountFieldsIntoContainer ----
          el.closest("div");
      }

      function movePaymentAmountFieldsIntoContainer() {
        var form = document.getElementById("formArea");
        if (!form) return;

        var payment = form.querySelector("#paymentMethodName");
        var total = form.querySelector("#totalAmount");
        var tax = form.querySelector("#taxAmount");

==============================
支払コンテナCSS確認
==============================

---- line 1710: receipt-payment-amount-container ----
      横並び維持に必要なコンテナgridだけ指定する。
    */

    #formArea .receipt-payment-amount-container {
      display: grid !important;
      grid-template-columns: 14rem 10rem 9rem max-content !important;
      gap: 0.5rem !important;
      align-items: end !important;

      width: calc(100% + 4rem) !important;

---- line 1737: receipt-payment-amount-container ----
    }

    @media (max-width: 86rem) {
      #formArea .receipt-payment-amount-container {
        width: 100% !important;
        max-width: 100% !important;
        margin-right: 0 !important;
      }
    }


---- line 4049: receipt-payment-amount-container ----
          }
        }

        var container = form.querySelector(".receipt-payment-amount-container");

        if (!container) {
          container = document.createElement("div");
          container.className = "receipt-payment-amount-container";

          var amountRow =

---- line 4053: receipt-payment-amount-container ----

        if (!container) {
          container = document.createElement("div");
          container.className = "receipt-payment-amount-container";

          var amountRow =
            totalBox.closest(".receipt-amount-direct-row") ||
            paymentBox.closest(".receipt-amount-direct-row") ||
            taxBox.closest(".receipt-amount-direct-row");


==============================
収集ファイル一覧
==============================
G:\GITHUB\wix-test\hd-origin-project\GPTが使う一時ファイルフォルダ\GPTに渡すフォルダ\web_receiver\public\receipts\receipt-list.html
G:\GITHUB\wix-test\hd-origin-project\GPTが使う一時ファイルフォルダ\GPTに渡すフォルダ\web_receiver\src\config.js
G:\GITHUB\wix-test\hd-origin-project\GPTが使う一時ファイルフォルダ\GPTに渡すフォルダ\web_receiver\src\db.js
