==============================
レシート台帳 左一覧 絞り込み欄追加 対象確認
==============================
日時: 2026-07-05 21:30:27

左一覧 id="list" の位置が見つからず、絞り込み欄を追加できませんでした。

[周辺]

  <!DOCTYPE html>
  <html lang="ja">
  <head>
    <meta charset="UTF-8">
    <title>レシート台帳</title>
>   <link rel="stylesheet" href="./receipt-list.css">
    <style>
      body {
        margin: 0;
        font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
        background: #f4f4f4;
      .scroll {
        height: calc(100% - 2.625rem);
        overflow: auto;
      }
  
>     .list-item {
        padding: 0.625rem;
        border-bottom: 0.0625rem solid #eee;
        cursor: pointer;
      }
  
>     .list-item:hover {
        background: #f0f4ff;
      }
  
>     .list-item.active {
        background: #e8f0ff;
        border-left: 0.25rem solid #333;
      }
  
>     .list-main {
        font-weight: 700;
        margin-bottom: 0.25rem;
      }
  
>     .list-sub {
        font-size: 0.75rem;
        color: #666;
        line-height: 1.5;
        word-break: break-all;
      }
      .evidence-info .field-value,
      .evidence-info .field div {
        word-break: break-all;
        white-space: normal;
      }
>     .list-line {
        display: flex;
        align-items: center;
        gap: 6px;
        white-space: nowrap;
        overflow: hidden;
        background: #fdecea;
        color: #b3261e;
        border: 1px solid #f3c0bc;
      }
  
>     .list-person {
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
      }
      .ledger-filter {
        font-size: 12px;
        color: #666;
        text-align: right;
      }
  
>     .list-line {
        display: flex;
        align-items: center;
        gap: 6px;
        white-space: nowrap;
        overflow: hidden;
        background: #fdecea;
        color: #b3261e;
        border: 1px solid #f3c0bc;
      }
  
>     .list-person {
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
      }
  </style>
  </head>
  <body>
    <header>
      <div>レシート台帳</div>
      <div class="receipt-header-actions">
>       <button type="button" onclick="location.href='/receipts/receipt-list.html'">読取確認へ戻る</button>
        <button type="button" onclick="loadSavedReceipts()">再読込</button>
        <button type="button" onclick="restartHdOriginServer()">サーバー再起動</button>
      </div>
    </header>
  
    <main>
      <section class="panel">
        <div class="panel-title">
>         <span>本保存済み</span>
        </div>
>       <div id="list" class="scroll">読込中...</div>
      </section>
  
      <section class="panel">
        <div class="panel-title">保存データ</div>
        <div id="detailArea" class="detail-area">左から選択してください。</div>
      function field(label, value) {
        return '<div class="field"><label>' + esc(label) + '</label><div>' + (value || "") + '</div></div>';
      }
  
      async function loadSavedReceipts() {
>       const list = document.getElementById("list");
>       list.innerHTML = "読込中...";
  
        const res = await fetch("/api/receipts/saved?limit=200");
        const data = await res.json().catch(() => ({ ok: false, error: "JSON読込失敗" }));
  
        if (!res.ok || !data.ok) {
>         list.innerHTML = '<div class="list-item warn">読込失敗: ' + esc(data.error || "") + '</div>';
          return;
        }
  
        currentItems = data.items || [];
  
        if (!currentItems.length) {
>         list.innerHTML = '<div class="list-item">本保存済みレシートはありません。</div>';
          return;
        }
  
>       list.innerHTML = currentItems.map((item) => {
          const active = Number(item.receipt_id) === Number(selectedReceiptId) ? " active" : "";
          return `
>           <div class="list-item${active}" onclick="loadSavedDetail(${esc(item.receipt_id)})">
>             <div class="list-line">
                <span>ID:${esc(item.receipt_id)}</span>
                <span>${esc(dateText(item.transaction_date))}</span>
                <span class="settled-badge ${item.is_settled ? "is-settled" : "is-unsettled"}">${item.is_settled ? "清算済み" : "未精算"}</s
pan>
>               <span class="list-person">/ ${esc(item.target_person_name || "")}</span>
              </div>
            </div>
          `;
        }).join("");
      }



